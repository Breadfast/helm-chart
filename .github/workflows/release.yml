name: 'release'
on:
  push:
    branches:
      - main
    paths:
    - 'service/**'
jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GIT_TOKEN }}
    steps:
      - uses: release-drafter/release-drafter@latest

